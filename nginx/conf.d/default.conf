# Staging backends (docker-compose-staging.yml on prisma_shared_net). Nginx runs only in staging.
# proxy_pass uses Docker DNS hostnames directly. Do not define upstream {} with the same name as
# those hostnames — nginx would treat proxy_pass http://name:8000 as an upstream + port, which is invalid.
# $connection_upgrade is defined in nginx.conf (avoid duplicating map here).

server {
    listen 80;
    server_name localhost 127.0.0.1;


    # Serve static files (mounted volume from ./nginx/html/static)
    location /static/ {
        alias /usr/share/nginx/html/static/;
        access_log off;
        expires 30d;
    }

    # ========================
    # Client App
    # ========================
    location /client/ws/ {
        proxy_pass http://client_staging_server:8000/ws/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location /client/ {
        proxy_pass http://client_staging_server:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # ========================
    # Detailer App
    # ========================
    location /detailer/ {
        proxy_pass http://detailer_staging_server:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # ========================
    # Admin Panel Routes
    # ========================
    location ~ ^/admin(/.*)?$ {
        proxy_pass http://client_staging_server:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # Detailer Admin Panel Routes
    location ~ ^/detailer-admin(/.*)?$ {
        proxy_pass http://detailer_staging_server:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        # WebSocket support
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # ========================
    # Support App (support docker-compose.staging — service support_staging_server)
    # ========================
    location /support/ {
        proxy_pass http://support_staging_server:8000/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    location ~ ^/support-admin(/.*)?$ {
        proxy_pass http://support_staging_server:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
    }

    # Client web SPA (client docker-compose-staging — service client_staging_web).
    # Trailing slash strips /app so the container still sees /assets and /index.html.
    location = /app {
        return 301 /app/;
    }
    location /app/ {
        proxy_pass http://client_staging_web/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
    }

    # Support desk SPA (support docker-compose-staging — service support_staging_web).
    # Trailing slash strips /desk so the container still sees /assets and /index.html.
    location = /desk {
        return 301 /desk/;
    }
    location /desk/ {
        proxy_pass http://support_staging_web/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
    }

     # Health check endpoint
    location = /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }
}
